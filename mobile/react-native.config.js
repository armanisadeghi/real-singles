/**
 * React Native CLI configuration.
 * 
 * Note: react-native-reanimated v4 requires react-native-worklets to provide
 * the native worklets runtime. Do NOT disable worklets native linking.
 */
module.exports = {
  dependencies: {
    // All dependencies autolinked by default
  },
};

