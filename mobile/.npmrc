# Fix for pnpm strict isolation causing Metro/Babel to fail
# when resolving nested dependencies like react-native-worklets/plugin
shamefully-hoist=true

# Ensure node_modules structure is compatible with Metro bundler
node-linker=hoisted
